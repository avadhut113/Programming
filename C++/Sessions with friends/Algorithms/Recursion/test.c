

int main1()
{
    char str[] = "Hello World!";
    int size = sizeof(str);
    int s = strlen(str);

    /*for (int first = 0, end = size - 1; first < size / 2; first++, end--) {
        std::swap(str[first], str[end]);
    }

    do_reverse(str, 0, size - 1);*/

    //std::cout << str << std::endl;

    //print_reverse(str, 0);
}